# AcceRL-Agent verified training runtime.
# Python 3.10 is recommended for this CUDA/PyTorch/vLLM stack.
#
# The grader/math-verification stack used by accerl_agent/scripts/drgrpo_grader.py
# is intentionally excluded.
#
# flash-attn imports torch while its wheel is being built. In a fresh
# environment, install torch first and then install this file with
# --no-build-isolation, as shown in QUICKSTART.md.
torch==2.11.0
transformers==5.12.1
vllm>=0.21.0
flash-attn==2.8.3.post1

ray[default]==2.56.0
textworld==1.7.0
tensorboard==2.21.0
safetensors==0.8.0
huggingface-hub==1.21.0
